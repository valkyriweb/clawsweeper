name: repair commit finding intake

on:
  repository_dispatch:
    types: [clawsweeper_commit_finding]
  workflow_dispatch:
    inputs:
      enabled:
        description: "Run commit finding intake"
        required: false
        default: "true"
        type: string
      target_repo:
        description: "Target repository"
        required: true
        default: "valkyriweb/clawsweeper"
        type: string
      commit_sha:
        description: "Commit SHA with a ClawSweeper finding"
        required: true
        type: string
      report_repo:
        description: "Repository containing the ClawSweeper report"
        required: false
        default: "valkyriweb/clawsweeper-state"
        type: string
      report_path:
        description: "Path to the ClawSweeper report"
        required: false
        default: ""
        type: string
      report_url:
        description: "Public report URL"
        required: false
        default: ""
        type: string
      runner:
        description: "Runner label for commit finding intake"
        required: false
        default: '["self-hosted","macOS","ARM64","mac-mini"]'
        type: string

permissions:
  contents: write
  actions: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  CLAWSWEEPER_APP_CLIENT_ID: Iv23lirdjmVqYd1gwY26
  CLAWSWEEPER_USAGE_TELEMETRY: "1"
  CLAWSWEEPER_USAGE_SERVICE_NAME: clawsweeper-runner
  # Set repo/org variable CLAWSWEEPER_USAGE_OTLP_ENDPOINT to broadcast usage events via OTLP.
  CLAWSWEEPER_USAGE_OTLP_ENDPOINT: ${{ vars.CLAWSWEEPER_USAGE_OTLP_ENDPOINT || vars.OTEL_EXPORTER_OTLP_ENDPOINT || '' }}
  CLAWSWEEPER_USAGE_OTLP_HEADERS: ${{ vars.CLAWSWEEPER_USAGE_OTLP_HEADERS || vars.OTEL_EXPORTER_OTLP_HEADERS || '' }}
  CLAWSWEEPER_USAGE_SERVICE_NAMESPACE: ${{ vars.CLAWSWEEPER_USAGE_SERVICE_NAMESPACE || vars.OTEL_SERVICE_NAMESPACE || '' }}
  OPIK_URL: https://opik.bermont.digital/api
  OPIK_PROJECT_NAME: clawsweeper
  CODEX_CLI_VERSION: ${{ vars.CLAWSWEEPER_CODEX_CLI_VERSION || '0.128.0' }}
  CLAWSWEEPER_ALLOWED_OWNER: ${{ vars.CLAWSWEEPER_ALLOWED_OWNER || 'valkyriweb' }}
  CLAWSWEEPER_ALLOW_EXECUTE: ${{ vars.CLAWSWEEPER_ALLOW_EXECUTE == '1' && '1' || '0' }}
  CLAWSWEEPER_ALLOW_FIX_PR: ${{ vars.CLAWSWEEPER_ALLOW_FIX_PR == '1' && '1' || '0' }}
  CLAWSWEEPER_ALLOW_MERGE: "0"
  CLAWSWEEPER_CODEX_REASONING_EFFORT: ${{ vars.CLAWSWEEPER_CODEX_REASONING_EFFORT || 'high' }}
  CLAWSWEEPER_CODEX_REVIEW_ATTEMPTS: ${{ vars.CLAWSWEEPER_CODEX_REVIEW_ATTEMPTS || '4' }}
  CLAWSWEEPER_FIX_CODEX_TIMEOUT_MS: ${{ vars.CLAWSWEEPER_FIX_CODEX_TIMEOUT_MS || '1200000' }}
  CLAWSWEEPER_FIX_STEP_TIMEOUT_MS: "2400000"
  CLAWSWEEPER_FIX_TIMEOUT_RESERVE_MS: ${{ vars.CLAWSWEEPER_FIX_TIMEOUT_RESERVE_MS || '300000' }}
  CLAWSWEEPER_FIX_PREFLIGHT_TIMEOUT_MS: ${{ vars.CLAWSWEEPER_FIX_PREFLIGHT_TIMEOUT_MS || '120000' }}
  CLAWSWEEPER_MODEL: ${{ vars.CLAWSWEEPER_MODEL || 'gpt-5.6-terra' }}
  CLAWSWEEPER_MODELS: ${{ vars.CLAWSWEEPER_MODELS || '' }}
  CLAWSWEEPER_TARGET_VALIDATION_MODE: ${{ vars.CLAWSWEEPER_TARGET_VALIDATION_MODE || 'changed-only' }}
  CLAWSWEEPER_RESOLVE_REVIEW_THREADS: ${{ vars.CLAWSWEEPER_RESOLVE_REVIEW_THREADS || '1' }}
  CLAWSWEEPER_MAX_ACTIVE_PRS_PER_AREA: ${{ vars.CLAWSWEEPER_MAX_ACTIVE_PRS_PER_AREA || '50' }}
  CLAWSWEEPER_GIT_USER_NAME: ${{ vars.CLAWSWEEPER_GIT_USER_NAME || 'clawsweeper[bot]' }}
  CLAWSWEEPER_GIT_USER_EMAIL: ${{ vars.CLAWSWEEPER_GIT_USER_EMAIL || '274271284+clawsweeper[bot]@users.noreply.github.com' }}

concurrency:
  group: commit-finding-${{ github.event.inputs.target_repo || github.event.client_payload.target_repo || 'valkyriweb/clawsweeper' }}-${{ github.event.inputs.commit_sha || github.event.client_payload.commit_sha || github.run_id }}
  cancel-in-progress: false

jobs:
  disabled-target:
    name: Skip disabled target commit finding
    if: ${{ github.event_name == 'repository_dispatch' && github.event.client_payload.target_repo == 'openclaw/clawhub' && vars.CLAWSWEEPER_ENABLE_CLAWHUB != '1' }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Explain skipped commit finding
        run: |
          echo "Skipping commit finding repair for ${{ github.event.client_payload.target_repo }}."
          echo "Set CLAWSWEEPER_ENABLE_CLAWHUB=1 after installing the ClawSweeper GitHub App on openclaw/clawhub."

  intake:
    if: ${{ !(github.event_name == 'repository_dispatch' && github.event.client_payload.target_repo == 'openclaw/clawhub' && vars.CLAWSWEEPER_ENABLE_CLAWHUB != '1') }}
    runs-on: ${{ fromJson(github.event.inputs.runner || vars.CLAWSWEEPER_COMMIT_FINDING_RUNNER || '["self-hosted","macOS","ARM64","mac-mini"]') }}
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@v6
        with:
          filter: blob:none
          fetch-depth: 0

      - uses: ./.github/actions/setup-pnpm
        with:
          build-script: build:repair

      - name: Resolve target repository
        id: target
        env:
          TARGET_REPO: ${{ github.event.inputs.target_repo || github.event.client_payload.target_repo || 'valkyriweb/clawsweeper' }}
        run: |
          set -euo pipefail
          {
            echo "repo=$TARGET_REPO"
            echo "owner=${TARGET_REPO%%/*}"
            echo "name=${TARGET_REPO#*/}"
          } >> "$GITHUB_OUTPUT"

      - name: Authorize legacy target token
        env:
          TARGET_REPO: ${{ steps.target.outputs.repo }}
        run: pnpm run --silent workflow -- legacy-target-auth --target-repo "$TARGET_REPO"

      - name: Create GitHub App token
        id: app_token
        uses: actions/create-github-app-token@1b10c78c7865c340bc4f6099eb2f838309f1e8c3 # v3.1.1
        with:
          client-id: ${{ env.CLAWSWEEPER_APP_CLIENT_ID }}
          private-key: ${{ secrets.CLAWSWEEPER_APP_PRIVATE_KEY }}
          owner: ${{ steps.target.outputs.owner }}
          repositories: ${{ steps.target.outputs.name }}
          permission-contents: write
          permission-issues: write
          permission-pull-requests: write
          permission-workflows: write
          permission-actions: read

      - name: Create state token
        id: state-token
        uses: ./.github/actions/create-state-token
        with:
          client-id: ${{ env.CLAWSWEEPER_APP_CLIENT_ID }}
          private-key: ${{ secrets.CLAWSWEEPER_APP_PRIVATE_KEY }}
          owner: valkyriweb
          repository: clawsweeper-state

      - uses: ./.github/actions/setup-state
        with:
          token: ${{ steps.state-token.outputs.token }}
          state-repository: valkyriweb/clawsweeper-state

      - name: Prepare commit finding intake
        id: prepare
        env:
          GH_TOKEN: ${{ steps.app_token.outputs.token }}
          ENABLED: ${{ github.event.inputs.enabled || github.event.client_payload.enabled || vars.CLAWSWEEPER_COMMIT_FINDING_INTAKE_ENABLED || 'true' }}
          TARGET_REPO: ${{ github.event.inputs.target_repo || github.event.client_payload.target_repo || 'valkyriweb/clawsweeper' }}
          COMMIT_SHA: ${{ github.event.inputs.commit_sha || github.event.client_payload.commit_sha }}
          REPORT_REPO: ${{ github.event.inputs.report_repo || github.event.client_payload.report_repo || 'valkyriweb/clawsweeper-state' }}
          REPORT_PATH: ${{ github.event.inputs.report_path || github.event.client_payload.report_path }}
          REPORT_URL: ${{ github.event.inputs.report_url || github.event.client_payload.report_url }}
        run: |
          set -euo pipefail
          args=(
            prepare
            --enabled "$ENABLED"
            --target-repo "$TARGET_REPO"
            --commit-sha "$COMMIT_SHA"
            --report-repo "$REPORT_REPO"
          )
          if [ -n "${REPORT_PATH:-}" ]; then
            args+=(--report-path "$REPORT_PATH")
          fi
          if [ -n "${REPORT_URL:-}" ]; then
            args+=(--report-url "$REPORT_URL")
          fi
          pnpm run repair:commit-finding-intake -- "${args[@]}"

      - uses: ./.github/actions/setup-codex
        if: ${{ steps.prepare.outputs.should_repair == 'true' }}
        with:
          version: ${{ env.CODEX_CLI_VERSION }}
          cache-suffix: target-pnpm
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

      - name: Review synthetic result
        if: ${{ steps.prepare.outputs.should_repair == 'true' }}
        run: pnpm run repair:review-results -- "${{ steps.prepare.outputs.result_path }}"

      - name: Execute credited fix artifact
        id: execute
        if: ${{ steps.prepare.outputs.should_repair == 'true' && env.CLAWSWEEPER_ALLOW_EXECUTE == '1' && env.CLAWSWEEPER_ALLOW_FIX_PR == '1' }}
        continue-on-error: true
        timeout-minutes: 40
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GH_TOKEN: ${{ steps.app_token.outputs.token }}
        run: pnpm run repair:execute-fix -- "${{ steps.prepare.outputs.job_path }}" "${{ steps.prepare.outputs.result_path }}"

      - name: Post-flight finalize fix PRs
        if: ${{ steps.prepare.outputs.should_repair == 'true' && steps.execute.outcome == 'success' && env.CLAWSWEEPER_ALLOW_EXECUTE == '1' && env.CLAWSWEEPER_ALLOW_FIX_PR == '1' }}
        env:
          GH_TOKEN: ${{ steps.app_token.outputs.token }}
        run: pnpm run repair:post-flight -- "${{ steps.prepare.outputs.job_path }}" "${{ steps.prepare.outputs.result_path }}"

      - name: Publish local result ledger
        if: ${{ always() && steps.prepare.outputs.should_repair == 'true' }}
        env:
          GH_TOKEN: ${{ steps.app_token.outputs.token }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          pnpm run repair:publish-result -- "${{ steps.prepare.outputs.run_dir }}" \
            --skip-dashboard \
            --run-id "${{ github.run_id }}" \
            --run-url "$RUN_URL" \
            --head-sha "${{ github.sha }}" \
            --conclusion "${{ steps.execute.outcome == 'success' && 'success' || steps.execute.outcome == 'failure' && 'failure' || 'skipped' }}"

      - name: Finalize commit finding audit
        if: always()
        run: |
          if [ -n "${{ steps.prepare.outputs.audit_path }}" ]; then
            pnpm run repair:commit-finding-intake -- finalize \
              --audit-path "${{ steps.prepare.outputs.audit_path }}" \
              --run-dir "${{ steps.prepare.outputs.run_dir }}" \
              --status "${{ steps.prepare.outputs.should_repair == 'true' && (steps.execute.outcome || 'skipped') || steps.prepare.outputs.status }}"
          fi

      - name: Commit intake ledger
        if: always()
        run: |
          set -euo pipefail
          pnpm run repair:publish-main -- \
            --message "chore: record commit finding intake" \
            --path jobs \
            --path results \
            --restore repair-apply-report.json \
            --max-attempts 12 \
            --push-attempts 4

      - name: Upload intake usage telemetry
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: clawsweeper-commit-finding-usage-${{ github.run_id }}-${{ github.run_attempt }}
          path: .clawsweeper-repair/runs/**/usage-events.jsonl
          if-no-files-found: warn
          retention-days: 7

      - name: Fail on repair failure
        if: ${{ steps.prepare.outputs.should_repair == 'true' && steps.execute.outcome == 'failure' }}
        run: exit 1
