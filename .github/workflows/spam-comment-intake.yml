name: spam comment intake

# Disabled 2026-06-01: spam intake isn't needed for Luke's private repos /
# orgs (no public comment surface to police). Implementation kept intact in
# case a repo is open-sourced later — re-enable by restoring the
# repository_dispatch trigger below (and re-enabling github-activity.yml,
# which sends the `github_activity` dispatch).
on:
  workflow_dispatch: {}
  # repository_dispatch:
  #   types: [github_activity]

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  CLAWSWEEPER_APP_CLIENT_ID: Iv23lirdjmVqYd1gwY26

concurrency:
  group: >-
    spam-comment-intake-${{ github.event.client_payload.target_repo || github.event.client_payload.activity.repo || 'valkyriweb/clawsweeper' }}-${{ github.event.client_payload.event_name || github.event.client_payload.activity.type || 'activity' }}-${{ github.event.client_payload.comment_id || github.event.client_payload.review_comment_id || github.event.client_payload.activity.comment.id || github.run_id }}
  cancel-in-progress: true

jobs:
  intake:
    if: >-
      ${{
        (contains(fromJSON('["issue_comment","pull_request_review_comment"]'), github.event.client_payload.activity.type) || contains(fromJSON('["issue_comment","pull_request_review_comment"]'), github.event.client_payload.event_name)) &&
        (github.event.client_payload.activity.action == 'created' || github.event.client_payload.activity.action == 'edited' || github.event.client_payload.action == 'created' || github.event.client_payload.action == 'edited')
      }}
    runs-on: blacksmith-4vcpu-ubuntu-2404
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
        with:
          filter: blob:none
          persist-credentials: false
          sparse-checkout: |
            .github/actions
            config/automation-limits.json
            src/repair
            src/model-registry.ts
            src/repository-profiles.ts
            src/review-tier-drift.ts
            src/usage-telemetry.ts
            package.json
            pnpm-lock.yaml
            tsconfig.repair.json
          sparse-checkout-cone-mode: false

      - name: Create GitHub App token
        id: app_token
        uses: actions/create-github-app-token@1b10c78c7865c340bc4f6099eb2f838309f1e8c3 # v3.1.1
        with:
          client-id: ${{ env.CLAWSWEEPER_APP_CLIENT_ID }}
          private-key: ${{ secrets.CLAWSWEEPER_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          permission-contents: write

      - uses: ./.github/actions/setup-pnpm
        with:
          build-script: build:repair

      - name: Dispatch exact spam scan
        env:
          GH_TOKEN: ${{ steps.app_token.outputs.token }}
        run: pnpm run repair:spam-comment-intake -- --write-report
