{
  "schema_version": 1,
  "review_routing": {
    "models": {
      "codex": "gpt-5.5",
      "claude-bridge": "claude-opus-4-8",
      "claude-code": "claude-opus-4-8",
      "pi": "claude-opus-4-8"
    }
  },
  "repositories": [
    {
      "target_repo": "bermont-digital/multica",
      "display_name": "Multica",
      "checkout_dir": "multica",
      "prompt_note": "Multica is an agent orchestration platform. Service areas: `area:backend-go` (Go server, pgx, database), `area:frontend-next` (Next.js views/packages), `area:daemon` (agent dispatcher, KUBECONFIG injection, task dispatch), `area:runtime` (Docker/K8s runtime, lue-kube deploy), `area:docs` (markdown docs, README). Use at most two area tags. When the item spans multiple areas, use `area:cross-cutting`. Validation: `cd server && go test ./...` for backend; `pnpm check` for frontend/packages.",
      "apply_close_rules": {
        "issue": [
          "implemented_on_main",
          "duplicate_or_superseded",
          "cannot_reproduce",
          "incoherent",
          "not_actionable_in_repo",
          "stale_insufficient_info"
        ],
        "pull_request": [
          "implemented_on_main",
          "mostly_implemented_on_main",
          "duplicate_or_superseded",
          "cannot_reproduce",
          "incoherent",
          "not_actionable_in_repo"
        ]
      }
    },
    {
      "target_repo": "CLIP-SA/core-ai",
      "display_name": "Core AI",
      "checkout_dir": "core-ai",
      "prompt_note": "Core AI is the backend AI/agent service for CLIP-SA. Service areas: `area:api` (REST/GraphQL endpoints), `area:ai-engine` (model routing, completions, tool dispatch), `area:auth` (authentication, session, permissions), `area:integrations` (OpenClaw, external service connectors), `area:infra` (Docker, CI, env config). Use at most two area tags. When the item spans multiple areas, use `area:cross-cutting`. Validation: use the repo's primary test and lint commands.",
      "apply_close_rules": {
        "issue": [
          "implemented_on_main",
          "duplicate_or_superseded",
          "cannot_reproduce",
          "incoherent",
          "not_actionable_in_repo",
          "stale_insufficient_info"
        ],
        "pull_request": [
          "implemented_on_main",
          "mostly_implemented_on_main",
          "duplicate_or_superseded",
          "cannot_reproduce",
          "incoherent",
          "not_actionable_in_repo"
        ]
      }
    },
    {
      "target_repo": "CLIP-SA/core-wholesale",
      "display_name": "Core Wholesale",
      "checkout_dir": "core-wholesale",
      "prompt_note": "Core Wholesale is a multi-stack ecommerce platform. Service areas: `area:next-frontend` (Next.js storefront, React components, pages), `area:woocommerce` (WooCommerce plugin customisations, hooks, REST API extensions), `area:wordpress` (WordPress core customisations, themes, ACF, plugin integrations), `area:mysql` (database schema, queries, migrations, performance), `area:openclaw-integration` (OpenClaw agent/AI integration, tool dispatch, session management). Use at most two area tags. When the item spans multiple areas, use `area:cross-cutting`. Validation: appropriate per area — PHP unit tests for WP/WC, `pnpm build` for Next.js, SQL migration checks for MySQL.",
      "apply_close_rules": {
        "issue": [
          "implemented_on_main",
          "duplicate_or_superseded",
          "cannot_reproduce",
          "incoherent",
          "not_actionable_in_repo",
          "stale_insufficient_info"
        ],
        "pull_request": [
          "implemented_on_main",
          "mostly_implemented_on_main",
          "duplicate_or_superseded",
          "cannot_reproduce",
          "incoherent",
          "not_actionable_in_repo"
        ]
      }
    },
    {
      "target_repo": "valkyriweb/pi-mono",
      "display_name": "Pi (mono-fork)",
      "checkout_dir": "pi-mono-fork",
      "include_maintainer_authored": true,
      "prompt_note": "Pi is Luke's fork of the Pi agent toolkit (coding agent CLI, unified LLM API, TUI/web UI libraries, Slack bot, vLLM pods). The local checkout lives at `~/Projects/personal/pi-mono-fork/`; the GitHub repo is `valkyriweb/pi-mono`. Service areas: `area:coding-agent` (the agent harness, session, tools, executor, context), `area:extensions` (dream-memory, pi-memory, claude-bridge, and other extension packages), `area:tui` (terminal UI, themes, keybindings, components), `area:sdk` (SDK integrations, custom providers, model additions), `area:docs` (markdown docs, examples, prompt templates), `area:tests` (unit and integration tests). Use at most two area tags. When the item spans multiple areas, use `area:cross-cutting`. Validation: `pnpm --filter coding-agent test` for harness changes; `pnpm -r build` for the monorepo. Default review provider stays Codex; Claude-bridge routing is opt-in per slice 7.",
      "apply_close_rules": {
        "issue": [
          "implemented_on_main",
          "duplicate_or_superseded",
          "cannot_reproduce",
          "incoherent",
          "not_actionable_in_repo",
          "stale_insufficient_info"
        ],
        "pull_request": [
          "implemented_on_main",
          "mostly_implemented_on_main",
          "duplicate_or_superseded",
          "cannot_reproduce",
          "incoherent",
          "not_actionable_in_repo"
        ]
      }
    },
    {
      "target_repo": "valkyriweb/my-pi",
      "display_name": "my-pi (extension & harness lab)",
      "checkout_dir": "my-pi",
      "include_maintainer_authored": true,
      "review_provider": "pi",
      "prompt_note": "my-pi is Luke's personal Pi extension and harness lab: custom extensions (pi-memory, dream-memory, claude-bridge markers, native-tool-overrides), the my-pi-full profile package, the Pi Setup Registry catalog, and memory/cache/context-strategy docs. It customizes Pi (forked at valkyriweb/pi-mono, local `~/Projects/personal/pi-mono-fork/`) via extensions/packages first; core fork patches only when documented APIs cannot achieve the behavior. The local checkout lives at `~/Projects/personal/my-pi/`; the GitHub repo is `valkyriweb/my-pi`. Service areas: `area:extensions` (extension packages + lifecycle), `area:packages` (my-pi-full profile + package metadata), `area:memory` (pi-memory/dream-memory architecture, recall, consolidation), `area:cache` (prompt-cache stability; system-prompt/tools byte-stability), `area:setup` (docs/pi-setup catalog YAML; generated registry), `area:docs` (markdown docs, ADRs), `area:tests` (extension-gate, evals). Use at most two area tags; `area:cross-cutting` when it spans many. Cache stability is a BLOCKING concern for any prompt-prefix/tools/messages change; pure TUI rendering is cache-irrelevant. Validation: `npm run test:extension-gate` for extension/profile/cache changes, `npm test` for root script tests, and the fork's `test:build-gate` must pass against the pinned pi-mono. Generated files under `docs/pi-setup/generated/` are scanner-owned — never hand-edit. Default review provider is Pi (Claude-bridge Opus), dogfooding pi-on-pi.",
      "apply_close_rules": {
        "issue": [
          "implemented_on_main",
          "duplicate_or_superseded",
          "cannot_reproduce",
          "incoherent",
          "not_actionable_in_repo",
          "stale_insufficient_info"
        ],
        "pull_request": [
          "implemented_on_main",
          "mostly_implemented_on_main",
          "duplicate_or_superseded",
          "cannot_reproduce",
          "incoherent",
          "not_actionable_in_repo"
        ]
      }
    },
    {
      "target_repo": "valkyriweb/horizon",
      "display_name": "horizon (product monorepo)",
      "checkout_dir": "horizon",
      "include_maintainer_authored": true,
      "prompt_note": "Horizon is Luke's product monorepo (Bun workspaces): `packages/*` + `apps/*`. Local checkout `~/Projects/work/horizon/`; GitHub repo `valkyriweb/horizon` (private). Packages: `packages/alerts` (Mailgun transactional email delivery), `packages/design` (design system), plus `shared` utilities. Apps: `apps/billing` (PayFast ITN/postback), `apps/whatsapp` (Meta WhatsApp send + webhook), `apps/mail-dashboard`, `apps/mail-landing`. Service areas: `area:alerts` (Mailgun delivery), `area:billing` (PayFast ITN confirmation, idempotency), `area:whatsapp` (Meta send/webhook/egress), `area:mail` (mail-dashboard + mail-landing), `area:design` (design system), `area:shared`, `area:ci`, `area:docs`. Use at most two area tags; `area:cross-cutting` when it spans many. The repo is mid Effect adoption: vendor boundaries (Mailgun, Meta, PayFast) are wrapped with typed errors, AbortSignal timeout, exp+jittered retry, and Effect Layers, while the Promise-facing public APIs stay behavior-identical — review for that contract and flag any public-API regression. Security-sensitive paths must fail closed: PayFast ITN server confirmation, SSRF egress validation (https-only; block private/link-local/metadata; post-DNS recheck), and per-route API-key scope enforcement (403 insufficient_scope). Validation: Bun workspaces — `bun install --frozen-lockfile`, then `bun run --filter '*' build`, `bun run --filter '*' typecheck`, `bun run --filter '*' test`; per package `bun test` + `tsc --noEmit`. For every PR review, include code-craft and agent-native-hardening passes: flag unnecessary complexity, poor naming/API shape, brittle agent-unfriendly structure, unsafe boundaries, and missing verification. Conservative close policy: this is a live product repo, so do not auto-close on implemented_on_main — Effect slices are intentionally reviewed-not-merged.",
      "apply_close_rules": {
        "issue": [],
        "pull_request": []
      }
    },
    {
      "target_repo": "valkyriweb/clawsweeper",
      "display_name": "ClawSweeper",
      "checkout_dir": "clawsweeper",
      "include_maintainer_authored": true,
      "prompt_note": "ClawSweeper is the GitHub issue/PR triage and repair automation in this repository. Service areas: `area:workflow` (GitHub Actions, tokens, permissions, dispatch), `area:repair` (issue intake, worker planning/execution, post-flight merge), `area:github-api` (gh/REST/GraphQL calls, search, permissions), `area:validation` (target validation command parsing), `area:tests` (unit and repair tests). Use at most two area tags. When fixing workflow bugs, prefer surgical changes with regression tests and validate with `pnpm run check` or the narrowest repair test/build gate.",
      "apply_close_rules": {
        "issue": [
          "implemented_on_main",
          "duplicate_or_superseded",
          "cannot_reproduce",
          "incoherent",
          "not_actionable_in_repo",
          "stale_insufficient_info"
        ],
        "pull_request": [
          "implemented_on_main",
          "mostly_implemented_on_main",
          "duplicate_or_superseded",
          "cannot_reproduce",
          "incoherent",
          "not_actionable_in_repo"
        ]
      }
    },
    {
      "target_repo": "valkyriweb/openclaw",
      "display_name": "OpenClaw (fork)",
      "checkout_dir": "openclaw",
      "include_maintainer_authored": true,
      "prompt_note": "Luke's patched fork of OpenClaw (agent gateway). Tracks upstream openclaw/openclaw with carry-forward patches tracked in ~/Projects/infra/openclaw-claude/docs/issues.md (ISSUE-040, ISSUE-042, ISSUE-077, ISSUE-091, ISSUE-094, ISSUE-095, cron-list-fix). Service areas: `area:agent-runtime` (ACP, agent loop, session lifecycle), `area:tools` (bash/exec, image_generate, web, sessions_*), `area:plugins` (advisor, context-intelligence, pi-ai), `area:bridge` (claude-bridge sidecar), `area:tests` (vitest). Use at most two area tags. When the item spans multiple areas, use `area:cross-cutting`. Validation: `pnpm exec vitest run <test-file>`. **CRITICAL: do NOT propose closing carry-forward PRs as 'implemented on main' — these are upstream-bound patches, not yet merged upstream.** Carry-forward PRs are typically named with `carry(<version>):` prefix; treat them as long-lived.",
      "apply_close_rules": {
        "issue": [
          "duplicate_or_superseded",
          "cannot_reproduce",
          "incoherent",
          "not_actionable_in_repo",
          "stale_insufficient_info"
        ],
        "pull_request": [
          "duplicate_or_superseded",
          "cannot_reproduce",
          "incoherent",
          "not_actionable_in_repo"
        ]
      }
    },
    {
      "target_repo": "valkyriweb/lue-kube",
      "display_name": "lue-kube (work cluster)",
      "checkout_dir": "lue-kube",
      "include_maintainer_authored": true,
      "prompt_note": "K3s cluster repo for client workloads (Laravel, WordPress, OpenClaw). Areas: `area:apps` (k3s/apps/<name>/{base,overlays/prod}), `area:flux` (Flux CD reconciliation), `area:cluster` (cluster-level config, system add-ons, sealed-secrets), `area:secrets`, `area:docs`. Use at most two area tags. Validation: `kubectl --context lue-kube` for cluster-side checks; manifests don't ship without `flux reconcile`. Carry-forward PRs do not exist here.",
      "apply_close_rules": {
        "issue": [
          "duplicate_or_superseded",
          "cannot_reproduce",
          "incoherent",
          "not_actionable_in_repo",
          "stale_insufficient_info"
        ],
        "pull_request": [
          "duplicate_or_superseded",
          "cannot_reproduce",
          "incoherent",
          "not_actionable_in_repo"
        ]
      }
    },
    {
      "target_repo": "valkyriweb/openclaw-claude",
      "display_name": "OpenClaw management hub (release/runtime ops)",
      "checkout_dir": "openclaw-claude",
      "review_provider": "pi",
      "include_maintainer_authored": true,
      "prompt_note": "OpenClaw management hub for Luke's production OpenClaw runtime: release notes, update logs, live-runtime issues, methodology, Paperclip wiring, plans, state, and docs. Mostly markdown; no source build. Areas: `area:release-risk`, `area:runtime-k8s`, `area:pi-harness`, `area:bridge-cache`, `area:skills-extensions`, `area:telemetry-evals`, `area:media-audio`, `area:context-health`, `area:docs-cleanup`, `area:methodology`. Use at most two area tags. Review as an ops/release triage assistant, not a generic stale-docs cleaner. Important work: read linked release logs/changelogs completely, compare against Luke's active runtime/cluster surfaces, and flag stability fixes, breaking config changes, Docker/Kubernetes/runtime impacts, prompt-cache/context health, Pi harness/bridge changes, skills/extensions, TTS/STT/media, heartbeats, telemetry/tracing/Opik/evals, active-memory, smoke/staging/build-test needs, and CHANGELOG/release tracking updates. Treat `docs/releases.md`, `docs/agent-context/update-release-runbook.md`, `state/update-log.jsonl`, `memory/MEMORY.md`, and `docs/issues.md` as operational state, not stale clutter. Prefer `manual_review` for release audits, runtime decisions, workflow design, plans, state logs, pending methodology, or anything needing cluster/operator judgment. Use `queue_fix_pr` only for narrow docs/index/changelog cleanup with an obvious existing file and no product/runtime decision; if no scoped validation command exists, choose `manual_review`. Do not propose closing methodology TODOs, pending-work notes, active plans, issue tracker entries, or release records as stale. Close only clear duplicates, cannot-reproduce reports, or items outside this repo boundary.",
      "apply_close_rules": {
        "issue": ["duplicate_or_superseded", "cannot_reproduce", "not_actionable_in_repo"],
        "pull_request": ["duplicate_or_superseded", "cannot_reproduce", "not_actionable_in_repo"]
      }
    }
  ]
}
